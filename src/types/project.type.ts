export interface projectCard {
  title: string;
  description: string;
  details: string;
  repo: string;
  imageUrl: string;
  mobile: boolean;
}

export interface programmingLanguages {
  name: string;
  icon: string;
}

export interface appRoutes {
  name: string,
  route: string
}
